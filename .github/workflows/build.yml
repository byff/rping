name: Build RPing

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        include:
          - target: x86_64-pc-windows-gnu
            os: ubuntu-latest
            name: rping-windows-x86_64.exe
            packages: gcc-mingw-w64-x86-64
            format: exe
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            name: rping-linux-x86_64
            packages: ""
            format: deb
            arch: amd64
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            name: rping-linux-aarch64
            packages: gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
            format: deb
            arch: arm64

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross-compilation tools
        if: matrix.packages != ''
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq ${{ matrix.packages }}

      - name: Install fpm (for .deb)
        if: matrix.format == 'deb'
        run: |
          sudo gem install fpm --no-document || sudo apt-get install -y -qq ruby ruby-dev rubygems build-essential
          sudo gem install fpm --no-document

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package .deb
        if: matrix.format == 'deb'
        run: |
          PKG_NAME="rping_{{VERSION}}-${{ matrix.arch }}.deb"
          fpm -s dir -t deb \
            -n rping \
            -v {{VERSION}} \
            -a ${{ matrix.arch }} \
            -p dist/${PKG_NAME} \
            -d "libc6" \
            --description "High-performance multi-target ping tool | Powered by byff" \
            --maintainer "byff" \
            --vendor "byff" \
            --license "MIT" \
            target/${{ matrix.target }}/release/rping=/usr/bin/rping

      - name: Package .exe
        if: matrix.format == 'exe'
        run: |
          mkdir -p dist
          cp target/${{ matrix.target }}/release/rping.exe dist/${{ matrix.name }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: dist/*

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
